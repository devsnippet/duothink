/**
 * Created by imdante on 2016/10/31.
 */
/**
 * 提交评论内容ajax操作
 * @returns {boolean}
 */
function create_new_reples(){
    var form = $('#reples-form');
    var message = form.find('#message').val();
    var url = form.attr('action');
    var formData = form.serialize();
    $('#create-new-reples').prepend('<i class="am-icon-spinner am-icon-pulse"></i>').addClass('am-disabled');
    if(message =='' || message.length < 10){
        showAlert(500,'评论内容不能为空 或是少于10个字符');
        setTimeout("reButtonState()", 5000);
        return false;
    }
    $.post(url,formData,function(callback){
        showAlert(callback.code,callback.msg,callback.code);
    })
    setTimeout("reButtonState()", 5000);
}
//重置评论表单状态
function reButtonState(){
    $('#create-new-reples').removeClass('am-disabled').find('i').remove();
}

/**
 *
 * @param e
 */
function followUser(e) {
    gform('/api_user_follow_user',{uid:e});
}
$(function(){
    $(document).on('click','.am-select-data-bar>ul>li',function(){
        var url = $(this).find('a').data('target');
        $(this).addClass('am-active').siblings('.am-select-data-bar>ul>li').removeClass('am-active');
        //console.log(url);
    })
})